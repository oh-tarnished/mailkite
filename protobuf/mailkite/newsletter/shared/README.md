# Shared Service

**Package:** `mailkite.newsletter.shared.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Messages

### NewsletterClientOptions

Client-level defaults a caller (e.g. mailkite's fan-out worker) can attach so common preferences aren't repeated on every request.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `default_page_size` | `int32` | `OPTIONAL` | Default page size for list endpoints when a request leaves page_size unset. Zero lets the server pick. |
| `default_order` | `OrderDirection` | `OPTIONAL` | Default sort direction for list endpoints. |
| `omit_bodies` | `bool` | `OPTIONAL` | When true, list/get calls omit large body fields by default. |
| `default_content_type` | `ContentType` | `OPTIONAL` | Default content type for created campaigns / messages that don't specify one. Falls back to CONTENT_TYPE_RICHTEXT. |
| `default_sender_email` | `string` | `OPTIONAL` | Default sender address for sends that leave sender_email empty. |
| `default_messenger` | `string` | `OPTIONAL` | Default messenger backend (e.g. "email") for sends without one. |

### DigestProjectionOptions

Defaults mailkite's fan-out applies when materializing a scheduled content digest into a Campaign. Stored alongside a tenant / list configuration.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `template` | `string` | `OPTIONAL` | Template to wrap every digest campaign in. Format: templates/{template} |
| `format` | `ContentType` | `OPTIONAL` | Authoring format digests are rendered as. |
| `tags` | `repeated string` | `OPTIONAL` | Tags stamped onto every generated digest campaign, for later filtering. |
| `publish_archive` | `bool` | `OPTIONAL` | Whether to publish each digest to the public archive automatically. |
| `max_items` | `int32` | `OPTIONAL` | Maximum number of content items per digest. Zero means no limit. |

## Enums

### ContentType

Enums shared across more than one Newsletter resource. Resource-specific enums live alongside their resource (e.g. CampaignStatus in the campaign package), so only the genuinely cross-cutting ones are defined here. Authoring format of campaign and transactional message bodies.

| Value | Number | Description |
| --- | --- | --- |
| `CONTENT_TYPE_UNSPECIFIED` | 0 | Server chooses a sensible default. |
| `CONTENT_TYPE_RICHTEXT` | 1 | Rich-text WYSIWYG (stored as HTML). |
| `CONTENT_TYPE_HTML` | 2 | Raw HTML. |
| `CONTENT_TYPE_MARKDOWN` | 3 | Markdown, rendered to HTML on send. |
| `CONTENT_TYPE_PLAIN` | 4 | Plain text. |
| `CONTENT_TYPE_VISUAL` | 5 | Block-based visual editor output. |

### OrderDirection

Sort direction for list endpoints.

| Value | Number | Description |
| --- | --- | --- |
| `ORDER_DIRECTION_UNSPECIFIED` | 0 | Server default (usually descending by creation time). |
| `ORDER_DIRECTION_ASC` | 1 | Ascending. |
| `ORDER_DIRECTION_DESC` | 2 | Descending. |

---

© 2026 oh-tarnished | Apache 2.0 License
