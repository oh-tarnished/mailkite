// Code generated by protoc-gen-protorm. DO NOT EDIT.
// versions:
// 	protoc-gen-protorm 1.0.0
// 	protoc             (unknown)
// source: mailkite/newsletter/media/v1/media.proto
//
// database: mailkite
// schemas:  media
//
// protorm — https://github.com/the-protobuf-project/protorm

/// An uploaded asset (image, attachment) referenced by campaigns and templates.
model Media {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The resource name of the media object. Format: media/{media}
  name String @unique @map("name")

  /// Server-assigned stable UUID.
  uuid String? @map("uuid")

  /// Original file name.
  filename String? @map("filename")

  /// MIME type (e.g. "image/png").
  mimeType String? @map("mime_type")

  /// Publicly servable URL of the asset.
  url String? @map("url")

  /// URL of the generated thumbnail, when one exists.
  thumbnailUrl String? @map("thumbnail_url")

  /// Upload timestamp.
  createTime DateTime @default(now()) @map("create_time")

  /// Maps the model to the "resource" table in the database.
  @@map("resource")
  /// Maps the model to the "media" schema in the database.
  @@schema("media")
}
